# Copia este archivo a .env y rellena los valores

# Puerto del servidor (Render lo asigna automáticamente en producción)
PORT=4000

# URL del frontend (para CORS - en producción usa tu dominio de Render)
FRONTEND_URL=http://localhost:5500

# JWT Secret para autenticación (CAMBIAR EN PRODUCCIÓN)
JWT_SECRET=your_jwt_secret_here_change_in_production

# Base de datos SQLite (ruta del archivo de base de datos)
SQLITE_FILE=./data/petconnect.db
